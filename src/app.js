import Vue from 'vue';

document.addEventListener('DOMContentLoaded', () => {
    new Vue({
        el: "#app",
        data: ["Create million dollar app", "Sell app", "Buy remote island"]
        }
    })
})